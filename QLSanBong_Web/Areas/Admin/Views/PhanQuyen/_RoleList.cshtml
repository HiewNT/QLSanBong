<style>
    /* Ẩn danh sách quyền khi mới tạo */
    .hidden {
        display: none;
    }

    /* Kiểu dáng cho phần tiêu đề dịch vụ có thể nhấn vào */
    .service-title {
        cursor: pointer;
        color: #525558; /* Màu xanh cho tiêu đề */
        display: flex;
        align-items: center;
    }

    .service-title:hover {
        color: #081829; /* Màu khi hover */
    }

    /* Mũi tên bên cạnh tên dịch vụ */
    .arrow-icon {
        margin-right: 10px; /* Khoảng cách giữa mũi tên và tên dịch vụ */
        font-size: 0.8em; /* Kích thước mũi tên */
    }

</style>


<table id="roleTable" class="table table-striped table-bordered display" style="width : 100%">
    <thead class="table-secondary">
        <tr>
            <th style="width: 30%; text-align: center;">ID</th> <!-- Cột ID -->
            <th style="width: 20%; text-align: center;">Vai trò</th> <!-- Cột Vai trò -->
            <th style="width: 20%; text-align: center;">Thông tin</th> <!-- Cột Thông tin -->
            <th style="width: 30%; text-align: center;">Thao tác</th> <!-- Cột Thao tác -->
        </tr>
    </thead>
    <tbody id="roleList"></tbody> <!-- Đảm bảo ID này chính xác -->
</table>


<!-- Modal Bootstrap -->
<div class="modal fade" id="roleModal" tabindex="-1" role="dialog" aria-labelledby="roleModalLabel" aria-hidden="true" data-bs-dismiss="modal">
    <div class="modal-dialog modal-xl" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="roleModalLabel">Các người dùng của vai trò</h5>
                <button type="button" class="close" data-bs-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <div id="modalContent"></div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Đóng</button>
            </div>
        </div>
    </div>
</div>

<!-- Modal Bootstrap -->
<div class="modal fade" id="authModal" tabindex="-1" role="dialog" aria-labelledby="authModalLabel"  data-roleid="">
    <div class="modal-dialog modal-l" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="authModalLabel">Quản lý quyền</h5>
                <button type="button" class="close" data-bs-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <div id="authTableContainer"></div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Đóng</button>
                <button style="display: none;" data-auth="phanquyen_edit" type="button" class="btn btn-primary" onclick="saveAuth()">Lưu thay đổi</button>
            </div>
        </div>
    </div>
</div>
